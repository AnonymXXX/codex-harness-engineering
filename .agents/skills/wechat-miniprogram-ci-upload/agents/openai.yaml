interface:
  display_name: "WeChat Mini Program CI Upload"
  short_description: "Open, name, preview, or upload WeChat Mini Programs safely."
  default_prompt: "Use $wechat-miniprogram-ci-upload to open or name the correct mp-weixin project, build and upload or preview the latest output, complete experience-version selection when requested, and keep Git publication separately authorized."
policy:
  allow_implicit_invocation: true
